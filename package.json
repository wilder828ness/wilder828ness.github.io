{
  "name": "nubztoys",
  "version": "1.0.0",
  "private": true,
  "description": "Nubz Toys & Collectibles storefront + static SEO page generator",
  "scripts": {
    "build": "node generate.js && tailwindcss -i ./src/tailwind-input.css -o ./css/tailwind.css --minify && node scripts/verify-css.js"
  },
  "dependencies": {
    "stripe": "^16.12.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  },
  "engines": {
    "node": ">=18"
  }
}
